{
  "name": "Functional - Health Check Endpoint",
  "description": "Ensures the /health endpoint returns a successful status and correct payload.",
  "test_type": "functional",
  "input": "GET /health",
  "output": "JSON response with success=true and status='ok'.",
  "success_criteria": [
    "HTTP status code is 200",
    "response['success'] is True",
    "response['status'] == 'ok'"
  ],
  "error_scenarios": null
}